# Vercel KV (required)
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_URL=

# Resend for email + PDF delivery (required for /api/email-capture)
RESEND_API_KEY=
RESEND_FROM_EMAIL="A11ySverige <noreply@a11ysverige.se>"

# Public site URL — used by PDF generation to fetch /report/[id]
# and as the canonical base for OpenGraph / sitemap / robots.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Plausible analytics (optional; script only loads if set)
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=
